var win = 0;
var ties = 0;
var losses = 0;

var options =[ "R" ,"P" ,"S"];


//Write a function to contain the game
var startGame = function () {
   //ask user for their choice or R P or S
var userChoice = window.prompt("Enter R, P, or S").toUpperCase()

var index = Math.floor(Math.random()* )

console.log(index)

if ( userChoice === index ){
    ties = ties 
    console.log("It's a tie!");
} else if (userChoice == 'R' && index == 'P'){
    losses++;
 console.log()
}else if (userChoice == 'R' && index == 'P'){

}else if (userChoice == 'R' && index == 'P'){

}


//if user does not make a choice end function immediatly

//convert our RPS from lower to upper (toUpperCase)

//use math.floor to grab a random index from our array options.length
var index = Math.floor(Math.random() = options.length)

//check user choice against comp choice to score the game (big if statement)

//score wins ties or loss ++ wins++

//ask user to play game again

//if user chosses play again run our function
 
}







//outside of our function call function to start game on launch
startGame();



var length = (prompt('choose R P S')
);
console.log(length)

if {}

alert('Must slect R P S');
return null ;

}